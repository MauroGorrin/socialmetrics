// Tailwind CSS v4 is configured CSS-first in `src/styles/globals.css`.
// This file exists for tooling that expects a config module and to scope
// content globs; design tokens live in the stylesheet's `@theme` block.
const config = {
  content: ['./src/**/*.{ts,tsx}'],
};

export default config;
